html(lang='en')
    head
        meta(charset='utf-8')
        meta(name='viewport', content='width=device-width, initial-scale=1, shrink-to-fit=no')
        meta(name='description', content='')
        meta(name='author', content='')
        title Crew Chat
        link(rel='icon', type='image/x-icon', href='assets/favicon.ico')
        script(src='https://use.fontawesome.com/releases/v6.3.0/js/all.js', crossorigin='anonymous')
        link(href='https://fonts.googleapis.com/css?family=Montserrat:400,700', rel='stylesheet', type='text/css')
        link(href='https://fonts.googleapis.com/css?family=Lato:400,700,400italic,700italic', rel='stylesheet', type='text/css')
        link(href='css/styles.css', rel='stylesheet')
        script(src='js/character-prompts.js')
        style.
            .chat-container {
                max-width: 800px;
                margin: 0 auto;
                height: 80vh;
                display: flex;
                flex-direction: column;
            }
            .chat-header {
                background: #2c3e50;
                color: white;
                padding: 1rem;
                text-align: center;
                border-radius: 10px 10px 0 0;
            }
            .chat-messages {
                flex: 1;
                overflow-y: auto;
                padding: 1rem;
                background: #f8f9fa;
                border-left: 1px solid #dee2e6;
                border-right: 1px solid #dee2e6;
            }
            .message {
                margin-bottom: 1rem;
                display: flex;
            }
            .message.user {
                justify-content: flex-end;
            }
            .message.bot {
                justify-content: flex-start;
            }
            .message-content {
                max-width: 70%;
                padding: 0.75rem 1rem;
                border-radius: 18px;
                word-wrap: break-word;
            }
            .message.user .message-content {
                background: #1abc9c;
                color: white;
            }
            .message.bot .message-content {
                background: white;
                color: #2c3e50;
                border: 1px solid #dee2e6;
            }
            .chat-input {
                display: flex;
                padding: 1rem;
                background: white;
                border: 1px solid #dee2e6;
                border-radius: 0 0 10px 10px;
                border-top: none;
            }
            .chat-input input {
                flex: 1;
                border: 1px solid #dee2e6;
                border-radius: 25px;
                padding: 0.75rem 1rem;
                margin-right: 0.5rem;
                outline: none;
            }
            .chat-input button {
                border-radius: 50%;
                width: 50px;
                height: 50px;
                border: none;
                background: #1abc9c;
                color: white;
                cursor: pointer;
                display: flex;
                align-items: center;
                justify-content: center;
            }
            .chat-input button:hover {
                background: #16a085;
            }
            .back-btn {
                position: fixed;
                top: 20px;
                left: 20px;
                z-index: 1000;
            }
            .typing-indicator .message-content {
                font-style: italic;
                opacity: 0.7;
            }
            .chat-input input:disabled,
            .chat-input button:disabled {
                opacity: 0.6;
                cursor: not-allowed;
            }

    body
        // Back button
        a.btn.btn-secondary.back-btn(href='index.html')
            i.fas.fa-arrow-left.me-2
            | Back to Home

        // Main content
        .container.mt-5.pt-5
            .chat-container
                .chat-header
                    h3#chatTitle Portfolio Chat
                .chat-messages#chatMessages
                    // Fake chat history
                    .message.bot
                        .message-content Hello! Welcome to my portfolio chat. How can I help you today?
                    .message.user
                        .message-content Hi! I'd like to know more about this project.
                    .message.bot
                        .message-content Great! This project showcases my skills in web development. What specific aspect would you like to know about?
                    .message.user
                        .message-content What technologies did you use?
                    .message.bot
                        .message-content I used HTML5, CSS3, JavaScript, and Bootstrap for the frontend. The backend was built with Node.js and Express.
                .chat-input
                    input#messageInput(type='text', placeholder='Type your message here...', maxlength='500')
                    button#sendButton(type='button')
                        i.fas.fa-paper-plane

        script(src='https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js')
        script(src='js/chat.js')